version: '3.8'

services:
  sonarqube:
    image: sonarqube:latest
    container_name: c6fd49a676674806aaf2af8aa5d97c5b_sonarqubelatest_d2e87b
    labels:
      - eae14b
    networks:
      github_network_5dce21196a4e403383bd5147199fbae9:
        aliases:
          - sonarqube
    ports:
      - "9000:9000"
    environment:
      - GITHUB_ACTIONS=true
      - CI=true
    healthcheck:
      test: ["CMD", "curl", "-s", "http://localhost:9000/api/system/health", "|", "grep", "-q", "UP"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_logs:/opt/sonarqube/logs
      - sonarqube_extensions:/opt/sonarqube/extensions
    ulimits:
      nproc: 131072
      nofile:
        soft: 65536
        hard: 131072

volumes:
  sonarqube_data:
  sonarqube_logs:
  sonarqube_extensions:

networks:
  github_network_5dce21196a4e403383bd5147199fbae9:
    name: github_network_5dce21196a4e403383bd5147199fbae9
